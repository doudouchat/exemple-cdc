logging:
   pattern:
      console: "%d %-5p: %C - %m%n"
   level:
      root: WARN
      com.exemple.cdc: DEBUG
      com.datastax.oss.driver: INFO
kafka:
   bootstrap-servers: 127.0.0.1:${kafka.client_port}
cassandra:
   port: ${cassandra.port}